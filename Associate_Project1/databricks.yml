# This is a Databricks asset bundle definition for Associate_Project1.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Associate_Project1
  uuid: 19b5c0e2-9c8f-4231-8075-7719d184389f

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-2ab0e8d6-d8fd.cloud.databricks.com
    variables:
      catalog: workspace
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-955bafe0-9eb9.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/karnpable5777@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/karnpable5777@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: karnpable5777@gmail.com
        level: CAN_MANAGE
