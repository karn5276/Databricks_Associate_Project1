create table associate_assignment.default.dim_master_customer(
    customer_id string,
    name string,
    city string,
    status string,
    updated_at timestamp,
    ingestion_timestamp timestamp,
    source_file_name string
);